# Local server
NODE_ENV=development
PORT=3000

# Local PostgreSQL, aligned with docker-compose.yml
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=location_booking
DB_PASSWORD=location_booking
DB_DATABASE=location_booking
DB_SSL=false

# TypeORM runtime behavior
DB_SYNCHRONIZE=false
DB_LOGGING=false
